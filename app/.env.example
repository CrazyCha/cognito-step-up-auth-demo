# Copy this file to .env and fill in values from `cdk deploy --outputs-file .cdk-outputs.json`

# From CDK outputs
USER_POOL_ID=us-east-1_XXXXXXXXX
CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
AWS_REGION=us-east-1

# Test user credentials (created by `node src/setup.js`)
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=TestPass1!

# Booking amount for the demo. Values above BOOKING_THRESHOLD (default: 5000) trigger step-up.
# Try 3000 (no step-up) and then 8000 (triggers step-up) to compare both paths.
BOOKING_AMOUNT=8000
